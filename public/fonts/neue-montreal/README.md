# Neue Montreal (Pangram Pangram)

**Neue Montreal** font files are loaded from this folder. Declarations are in `src/app/globals.css`.

Supported formats: `.otf` (current) or `.woff2` (smaller, update globals.css if switching).

Neue Montreal is a commercial font from [Pangram Pangram](https://pangrampangram.com).
